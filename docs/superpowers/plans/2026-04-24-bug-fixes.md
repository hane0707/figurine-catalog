# バグ・仕様不備修正 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `docs/修正指示.md` の「## バグ・仕様不備」7項目をすべて修正する

**Architecture:** (1) 詳細ページの認証ガードを `isPublic` 考慮に変更し、API レベルでも非公開フィルタを追加。(2) テンプレートの UI 不備（ボタン重複・登録カード非表示）を修正し、鍵アイコンと画像クリック切り替えを追加。(3) 編集パネルに `PhotoUploader` を組み込んで画像追加・削除を実現する。

**Tech Stack:** SvelteKit (Svelte 5 runes), TypeScript, Drizzle ORM, Cloudflare Workers, Vitest + Testing Library

---

## ファイル構成

| ファイル | 変更種別 | バグ番号 |
|---------|---------|---------|
| `src/routes/items/[id]/+page.server.ts` | 修正 | #1・#6 |
| `src/routes/items/[id]/page.server.test.ts` | 修正 | #1・#6 |
| `src/routes/api/items/+server.ts` | 修正 | #6 |
| `src/routes/items/+page.svelte` | 修正 | #2・#7 |
| `src/lib/components/ItemCard.svelte` | 修正 | #7 |
| `src/routes/items/[id]/+page.svelte` | 修正 | #3・#4・#5・#7 |
| `src/routes/items/[id]/page.test.ts` | 修正 | #3 |

---

## Task 1: 認証ガード — テスト先行

**Files:**
- Modify: `src/routes/items/[id]/page.server.test.ts`

- [ ] **Step 1: テストファイルを以下の内容で全置き換えする**

```typescript
// src/routes/items/[id]/page.server.test.ts
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { load } from './+page.server';

const mockFindFirst = vi.fn();

vi.mock('$lib/server/db', () => ({
  getDb: vi.fn(() => ({
    query: { items: { findFirst: mockFindFirst } },
    select: vi.fn(() => ({
      from: vi.fn(() => {
        const p = Promise.resolve([]);
        return Object.assign(p, { orderBy: vi.fn().mockResolvedValue([]) });
      }),
    })),
  })),
  items: {},
  tags: {},
  materials: {},
}));

vi.mock('$lib/server/r2', () => ({
  getPresignedGetUrl: vi.fn().mockResolvedValue('https://cdn.example.com/photo.jpg'),
}));

const baseItem = {
  id: 'item-1',
  name: 'テスト',
  series: null,
  isPublic: 0,
  isHandmade: null,
  status: 'owned',
  createdAt: '2026-01-01',
  updatedAt: '2026-01-01',
  photos: [],
  purchaseInfo: null,
  handmadeInfo: null,
  itemTags: [],
  itemMaterials: [],
};

const ctx = (locals: Record<string, unknown> = {}) => ({
  params: { id: 'item-1' },
  platform: { env: { DB: {} } },
  locals,
});

describe('/items/[id] load: 認証ガード', () => {
  beforeEach(() => vi.clearAllMocks());

  it('未ログイン + 非公開アイテム → 404', async () => {
    mockFindFirst.mockResolvedValue({ ...baseItem, isPublic: 0 });
    await expect(load(ctx() as any)).rejects.toMatchObject({ status: 404 });
  });

  it('未ログイン + 公開アイテム → data を返す', async () => {
    mockFindFirst.mockResolvedValue({ ...baseItem, isPublic: 1 });
    const result = await load(ctx() as any);
    expect(result.item.id).toBe('item-1');
  });

  it('ログイン済み + 非公開アイテム → data を返す', async () => {
    mockFindFirst.mockResolvedValue({ ...baseItem, isPublic: 0 });
    const result = await load(ctx({ user: { email: 'test@example.com' } }) as any);
    expect(result.item.id).toBe('item-1');
  });

  it('アイテムが存在しない → 404', async () => {
    mockFindFirst.mockResolvedValue(null);
    await expect(
      load(ctx({ user: { email: 'test@example.com' } }) as any)
    ).rejects.toMatchObject({ status: 404 });
  });
});
```

- [ ] **Step 2: テストを実行してすべて FAIL を確認**

```bash
cd /home/haku/projects/figurine-catalog && npx vitest run src/routes/items/[id]/page.server.test.ts
```

期待: 4テスト全て FAIL（現在のコードは302リダイレクトするため未ログインテストが通らない）

---

## Task 2: 認証ガード実装 (#1・#6)

**Files:**
- Modify: `src/routes/items/[id]/+page.server.ts`
- Modify: `src/routes/api/items/+server.ts`

- [ ] **Step 1: `+page.server.ts` の認証ガードを変更する**

ファイル全体を以下で置き換える:

```typescript
// src/routes/items/[id]/+page.server.ts
import { error } from '@sveltejs/kit';
import type { PageServerLoad } from './$types';
import { getDb, items, tags, materials } from '$lib/server/db';
import { eq } from 'drizzle-orm';
import { getPresignedGetUrl } from '$lib/server/r2';

export const load: PageServerLoad = async ({ params, locals, platform }) => {
  const db = getDb(platform!.env.DB);

  const [item, allTags, allMaterials] = await Promise.all([
    db.query.items.findFirst({
      where: eq(items.id, params.id),
      with: {
        photos: { orderBy: (p, { asc }) => [asc(p.sortOrder)] },
        purchaseInfo: true,
        handmadeInfo: true,
        itemTags: { with: { tag: true } },
        itemMaterials: { with: { material: true } },
      },
    }),
    db.select().from(tags).orderBy(tags.name),
    db.select().from(materials),
  ]);

  if (!item) throw error(404, 'アイテムが見つかりません');
  if (!locals.user && !item.isPublic) throw error(404, 'アイテムが見つかりません');

  const photosWithUrls = await Promise.all(
    item.photos.map(async (p) => ({
      ...p,
      thumbUrl: await getPresignedGetUrl(platform!.env, p.r2KeyThumb),
      origUrl: await getPresignedGetUrl(platform!.env, p.r2KeyOrig),
    }))
  );

  const frequent = allMaterials.filter((m) => m.isPreset).slice(0, 6);

  return {
    item: { ...item, photos: photosWithUrls },
    allTags,
    materials: { all: allMaterials, frequent },
  };
};
```

- [ ] **Step 2: `api/items/+server.ts` の GET ハンドラを変更する**

`src/routes/api/items/+server.ts` の GET ハンドラ（35行目〜）を以下で置き換える（POST ハンドラは変更なし）:

```typescript
export const GET: RequestHandler = async ({ url, platform, locals }) => {
  const db = getDb(platform!.env.DB);
  const limit = Math.min(Number(url.searchParams.get('limit') ?? 30), 100);
  const offset = Number(url.searchParams.get('offset') ?? 0);
  const status = url.searchParams.get('status') ?? 'owned';
  const q = url.searchParams.get('q') ?? '';
  const tagsParam = url.searchParams.get('tags') ?? '';
  const tagIds = tagsParam ? tagsParam.split(',').filter(Boolean) : [];
  const kind = url.searchParams.get('kind') ?? 'all';
  const sort = url.searchParams.get('sort') ?? 'recent';

  const tagFilter = tagIds.length > 0
    ? exists(
        db.select({ one: sql`1` })
          .from(itemTags)
          .where(and(eq(itemTags.itemId, items.id), inArray(itemTags.tagId, tagIds)))
      )
    : undefined;

  const kindFilter =
    kind === 'bought' ? eq(items.isHandmade, 0) :
    kind === 'handmade' ? eq(items.isHandmade, 1) :
    undefined;

  const publicFilter = locals.user ? undefined : eq(items.isPublic, 1);

  const rows = await db
    .select({
      id: items.id,
      name: items.name,
      series: items.series,
      isHandmade: items.isHandmade,
      status: items.status,
      isPublic: items.isPublic,
      createdAt: items.createdAt,
      r2KeyThumb: photos.r2KeyThumb,
    })
    .from(items)
    .leftJoin(photos, and(eq(photos.itemId, items.id), eq(photos.isCover, 1)))
    .where(
      and(
        eq(items.status, status),
        q ? or(like(items.name, `%${q}%`), like(items.series, `%${q}%`)) : undefined,
        tagFilter,
        kindFilter,
        publicFilter,
      )
    )
    .orderBy(sort === 'oldest' ? asc(items.createdAt) : desc(items.createdAt))
    .limit(limit)
    .offset(offset);

  const itemsWithUrls = await Promise.all(
    rows.map(async (row) => ({
      id: row.id,
      name: row.name,
      series: row.series,
      isHandmade: row.isHandmade,
      status: row.status,
      isPublic: row.isPublic,
      createdAt: row.createdAt,
      thumbUrl: row.r2KeyThumb
        ? await getPresignedGetUrl(platform!.env, row.r2KeyThumb)
        : null,
    }))
  );

  return json({ items: itemsWithUrls, offset, limit });

  return json({ items: itemsWithUrls, offset, limit });
};
```

- [ ] **Step 3: テストを実行して PASS を確認**

```bash
cd /home/haku/projects/figurine-catalog && npx vitest run src/routes/items/[id]/page.server.test.ts
```

期待: 4テスト全て PASS

- [ ] **Step 4: コミット**

```bash
cd /home/haku/projects/figurine-catalog && git add src/routes/items/[id]/+page.server.ts src/routes/items/[id]/page.server.test.ts src/routes/api/items/+server.ts && git commit -m "fix: 詳細ページ認証ガードを isPublic 対応に変更し API の非公開フィルタを追加"
```

---

## Task 3: 未ログイン時 UI 制御 (#2)

**Files:**
- Modify: `src/routes/items/+page.svelte`

- [ ] **Step 1: 「新しいアイテムを登録」カードを `{#if data.user}` で囲む**

`src/routes/items/+page.svelte` の 250〜255行目の以下のブロックを:

```svelte
      <a href="/items/new" class="card --empty">
        <div class="plus">+</div>
        <div style="font-family:var(--f-mono); font-size:11px; letter-spacing:0.12em; text-transform:uppercase">
          新しいアイテムを登録
        </div>
      </a>
```

以下に置き換える:

```svelte
      {#if data.user}
        <a href="/items/new" class="card --empty">
          <div class="plus">+</div>
          <div style="font-family:var(--f-mono); font-size:11px; letter-spacing:0.12em; text-transform:uppercase">
            新しいアイテムを登録
          </div>
        </a>
      {/if}
```

- [ ] **Step 2: コミット**

```bash
cd /home/haku/projects/figurine-catalog && git add src/routes/items/+page.svelte && git commit -m "fix: 未ログイン時に「新しいアイテムを登録」カードを非表示にする"
```

---

## Task 4: ボタン重複削除 (#3)

**Files:**
- Modify: `src/routes/items/[id]/page.test.ts`
- Modify: `src/routes/items/[id]/+page.svelte`

- [ ] **Step 1: テストに「ボタンは各1つだけ」アサーションを追加して FAIL を確認**

`src/routes/items/[id]/page.test.ts` の `describe('編集画面: 表示/編集の二重表示', ...)` ブロックの最後に以下を追加:

```typescript
  it('編集・削除ボタンが各1つだけ表示される', () => {
    render(Page, { data: mockData });
    expect(screen.getAllByText('編集')).toHaveLength(1);
    expect(screen.getAllByText('削除')).toHaveLength(1);
  });
```

```bash
cd /home/haku/projects/figurine-catalog && npx vitest run src/routes/items/[id]/page.test.ts
```

期待: 新しいテストのみ FAIL（現在はボタンが2つある）

- [ ] **Step 2: `+page.svelte` から `sheet-actions` ブロックを削除する**

`src/routes/items/[id]/+page.svelte` の 256〜264行目の以下のブロックを削除:

```svelte
        <!-- アクション (ログイン時のみ) -->
        {#if data.user}
          <div class="sheet-actions">
            <div class="sheet-ops">
              <button class="btn --ghost --danger" onclick={deleteItem}>削除</button>
              <button class="btn --primary" onclick={startEdit}>編集</button>
            </div>
          </div>
        {/if}
```

- [ ] **Step 3: 既存テストの `getAllByText('編集')[0]` を `getByText('編集')` に修正する**

`src/routes/items/[id]/page.test.ts` 内の以下の3箇所:

```typescript
const editButton = screen.getAllByText('編集')[0];
```

をすべて:

```typescript
const editButton = screen.getByText('編集');
```

に変更する。

- [ ] **Step 4: テストを実行して全件 PASS を確認**

```bash
cd /home/haku/projects/figurine-catalog && npx vitest run src/routes/items/[id]/page.test.ts
```

期待: 全テスト PASS

- [ ] **Step 5: コミット**

```bash
cd /home/haku/projects/figurine-catalog && git add src/routes/items/[id]/+page.svelte src/routes/items/[id]/page.test.ts && git commit -m "fix: 詳細ページの編集・削除ボタン重複を修正"
```

---

## Task 5: 鍵アイコン表示 (#7)

**Files:**
- Modify: `src/lib/components/ItemCard.svelte`
- Modify: `src/routes/items/+page.svelte`
- Modify: `src/routes/items/[id]/+page.svelte`

- [ ] **Step 1: `ItemCard.svelte` に `isOwner` プロップと鍵アイコンを追加する**

ファイル全体を以下で置き換える:

```svelte
<!-- src/lib/components/ItemCard.svelte -->
<script lang="ts">
  let { item, isOwner = false }: {
    item: {
      id: string;
      name: string | null;
      series?: string | null;
      isHandmade?: number | null;
      thumbUrl: string | null;
      isPublic: number;
      status: string;
      createdAt?: string | null;
    };
    isOwner?: boolean;
  } = $props();

  const kindLabel = item.isHandmade === 1 ? 'Handmade' : 'Collected';
</script>

<a href="/items/{item.id}" class="card">
  <div class="card-img">
    {#if item.thumbUrl}
      <img src={item.thumbUrl} alt={item.name ?? '名称未設定'} loading="lazy" />
    {:else}
      <div style="width:100%; height:100%; display:grid; place-items:center; font-family:var(--f-display); font-size:40px; opacity:0.2; color:var(--fg)">✦</div>
    {/if}
    {#if item.isHandmade !== undefined && item.isHandmade !== null}
      <div class={'card-badge ' + (item.isHandmade === 1 ? '--handmade' : '--bought')}>
        {kindLabel}
      </div>
    {/if}
    {#if isOwner && item.isPublic === 0}
      <div style="position:absolute; top:8px; right:8px; width:22px; height:22px; border-radius:50%; background:rgba(0,0,0,0.45); display:grid; place-items:center; color:#fff">
        <svg width="11" height="11" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
          <rect x="3" y="11" width="18" height="11" rx="2" ry="2"/>
          <path d="M7 11V7a5 5 0 0 1 10 0v4"/>
        </svg>
      </div>
    {/if}
  </div>
  <h3>{item.name ?? '名称未設定'}</h3>
  <p class="card-series">{item.series ?? '—'}</p>
  <div class={'card-meta ' + (item.isHandmade === 1 ? '' : '--haze')}>
    <span>
      <span class="dot"></span>
      {item.isHandmade === 1 ? 'HANDMADE' : item.isHandmade === 0 ? 'COLLECTED' : 'ITEM'}
    </span>
    <span class="mono" style="font-size:10px">{item.createdAt?.slice(0, 10) ?? ''}</span>
  </div>
</a>
```

- [ ] **Step 2: `items/+page.svelte` の `ItemCard` に `isOwner` を渡す**

`src/routes/items/+page.svelte` の 248行目:

```svelte
        <ItemCard {item} />
```

を以下に変更:

```svelte
        <ItemCard {item} isOwner={!!data.user} />
```

- [ ] **Step 3: 詳細ページのタイトルに鍵アイコンを追加する**

`src/routes/items/[id]/+page.svelte` の 200行目:

```svelte
          <h1>{item.name ?? '名称未設定'}</h1>
```

を以下に変更:

```svelte
          <h1 style="display:flex; align-items:center; gap:8px">
            {item.name ?? '名称未設定'}
            {#if data.user && !item.isPublic}
              <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor"
                   stroke-width="2" stroke-linecap="round" stroke-linejoin="round"
                   style="color:var(--fg-mute); flex-shrink:0; margin-top:2px">
                <rect x="3" y="11" width="18" height="11" rx="2" ry="2"/>
                <path d="M7 11V7a5 5 0 0 1 10 0v4"/>
              </svg>
            {/if}
          </h1>
```

- [ ] **Step 4: コミット**

```bash
cd /home/haku/projects/figurine-catalog && git add src/lib/components/ItemCard.svelte src/routes/items/+page.svelte src/routes/items/[id]/+page.svelte && git commit -m "feat: ログイン時に非公開アイテムの鍵アイコンを一覧・詳細ページに表示"
```

---

## Task 6: 画像クリック切り替え (#4)

**Files:**
- Modify: `src/routes/items/[id]/+page.svelte`

- [ ] **Step 1: `selectedPhoto` の状態管理を追加する**

`src/routes/items/[id]/+page.svelte` の 132〜134行目（`<script>` ブロック末尾）の以下:

```typescript
  let coverPhoto = $derived(item.photos.find((p: any) => p.isCover) ?? item.photos[0]);
  let otherPhotos = $derived(item.photos.filter((p: any) => p !== coverPhoto).slice(0, 3));
  const kindLabel = $derived(item.isHandmade === 1 ? 'Handmade' : item.isHandmade === 0 ? 'Collected' : 'Item');
```

を以下に変更:

```typescript
  let coverPhoto = $derived(item.photos.find((p: any) => p.isCover) ?? item.photos[0]);
  let selectedPhoto = $state(coverPhoto);
  $effect(() => { selectedPhoto = coverPhoto; });
  let otherPhotos = $derived(item.photos.filter((p: any) => p !== selectedPhoto).slice(0, 3));
  const kindLabel = $derived(item.isHandmade === 1 ? 'Handmade' : item.isHandmade === 0 ? 'Collected' : 'Item');
```

- [ ] **Step 2: メイン画像表示と左パネルのサムネイルをクリック可能に変更する**

`src/routes/items/[id]/+page.svelte` の `detail-img-panel` ブロック（174〜193行目）を以下で置き換える:

```svelte
    <!-- 左：画像パネル -->
    <div class="detail-img-panel">
      {#if selectedPhoto}
        <img src={selectedPhoto.thumbUrl} alt={item.name ?? ''} />
        <div class="overlay-tag">
          {kindLabel} · {item.createdAt?.slice(0, 10) ?? ''}
        </div>
        {#if otherPhotos.length > 0}
          <div class="thumbs">
            {#each otherPhotos as photo}
              <button
                class="t"
                onclick={() => (selectedPhoto = photo)}
                style="background:none; border:none; padding:0; cursor:pointer"
              >
                <img src={photo.thumbUrl} alt="" />
              </button>
            {/each}
          </div>
        {/if}
      {:else}
        <div style="width:100%; height:100%; display:grid; place-items:center; background:var(--bg-sunk)">
          <span style="font-family:var(--f-display); font-size:72px; opacity:0.15; color:var(--fg)">✦</span>
        </div>
        <div class="overlay-tag">{kindLabel}</div>
      {/if}
    </div>
```

- [ ] **Step 3: 下部「All Photos」グリッドをクリック可能にする**

`src/routes/items/[id]/+page.svelte` の 353〜365行目の `All Photos` セクションを以下で置き換える:

```svelte
  <!-- 全写真グリッド -->
  {#if item.photos.length > 1}
    <div style="margin-top:48px">
      <div class="eyebrow" style="margin-bottom:16px">All Photos</div>
      <div style="display:grid; grid-template-columns:repeat(auto-fill, minmax(120px, 1fr)); gap:12px">
        {#each item.photos as photo}
          <button
            onclick={() => (selectedPhoto = photo)}
            style="aspect-ratio:1; border-radius:var(--radius-sm); overflow:hidden; box-shadow:var(--neu-soft); background:none; border:none; padding:0; cursor:pointer; display:block; width:100%"
          >
            <img src={photo.thumbUrl} alt="" style="width:100%; height:100%; object-fit:cover; display:block" />
          </button>
        {/each}
      </div>
    </div>
  {/if}
```

- [ ] **Step 4: コミット**

```bash
cd /home/haku/projects/figurine-catalog && git add src/routes/items/[id]/+page.svelte && git commit -m "feat: 詳細ページでサムネクリックによるメイン画像切り替えを実装"
```

---

## Task 7: 編集時の画像管理 (#5)

**Files:**
- Modify: `src/routes/items/[id]/+page.svelte`

- [ ] **Step 1: `PhotoUploader` のインポートを追加する**

`src/routes/items/[id]/+page.svelte` の `<script>` ブロック先頭（3行目付近）のインポート群に追加:

```typescript
  import PhotoUploader from '$lib/components/PhotoUploader.svelte';
```

- [ ] **Step 2: `editPhotos` 状態と写真管理関数を追加する**

`src/routes/items/[id]/+page.svelte` の `let editMaterials = $state<...>([]);` の直後（30行目付近）に追加:

```typescript
  let editPhotos = $state<Array<{ id: string; thumbUrl: string }>>([]);
```

`startEdit()` 関数内の `editing = true;` の直前に追加:

```typescript
    editPhotos = item.photos.map((p: any) => ({ id: p.id, thumbUrl: p.thumbUrl }));
```

`createMaterial` 関数（118行目付近）の直後に以下を追加:

```typescript
  async function deleteEditPhoto(photoId: string) {
    try {
      const res = await fetch(`/api/photos/${photoId}`, { method: 'DELETE' });
      if (!res.ok) throw new Error(`削除失敗: ${res.status}`);
      editPhotos = editPhotos.filter((p) => p.id !== photoId);
    } catch (e) {
      toast.error('写真の削除に失敗しました');
      console.error(e);
    }
  }

  async function handlePhotoUploaded(
    photo: { id: string; r2KeyOrig: string; r2KeyThumb: string; thumbViewUrl: string },
  ) {
    try {
      const res = await fetch(`/api/photos/${photo.id}`, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ itemId: item.id, r2KeyOrig: photo.r2KeyOrig, r2KeyThumb: photo.r2KeyThumb }),
      });
      if (!res.ok) throw new Error(`登録失敗: ${res.status}`);
      editPhotos = [...editPhotos, { id: photo.id, thumbUrl: photo.thumbViewUrl }];
    } catch (e) {
      toast.error('写真の追加に失敗しました');
      console.error(e);
    }
  }
```

- [ ] **Step 3: 編集パネルに写真セクションを追加する**

`src/routes/items/[id]/+page.svelte` の 編集パネル内（`<!-- 編集モード -->` ブロック）で、最初の `<div class="edit-section">` （`<div class="edit-section-title">基本情報</div>` を含むブロック）の直前に以下を挿入:

```svelte
          <div class="edit-section">
            <div class="edit-section-title">写真</div>
            {#if editPhotos.length > 0}
              <div style="display:grid; grid-template-columns:repeat(auto-fill, minmax(80px, 1fr)); gap:8px; margin-bottom:12px">
                {#each editPhotos as photo (photo.id)}
                  <div style="position:relative; aspect-ratio:1; border-radius:var(--radius-sm); overflow:hidden; box-shadow:var(--neu-soft)">
                    <img src={photo.thumbUrl} alt="" style="width:100%; height:100%; object-fit:cover" />
                    <button
                      type="button"
                      onclick={() => deleteEditPhoto(photo.id)}
                      style="position:absolute; top:4px; right:4px; width:20px; height:20px; border-radius:50%; background:rgba(0,0,0,0.6); color:#fff; border:none; cursor:pointer; display:grid; place-items:center; font-size:12px; line-height:1; padding:0"
                    >×</button>
                  </div>
                {/each}
              </div>
            {/if}
            <PhotoUploader
              itemId={item.id}
              itemCreated={true}
              onUploaded={handlePhotoUploaded}
              onSystemError={() => toast.error('アップロードに失敗しました')}
            />
          </div>
```

- [ ] **Step 4: 全テストを実行して PASS を確認**

```bash
cd /home/haku/projects/figurine-catalog && npx vitest run
```

期待: 全テスト PASS

- [ ] **Step 5: コミット**

```bash
cd /home/haku/projects/figurine-catalog && git add src/routes/items/[id]/+page.svelte && git commit -m "feat: 編集モードでアイテム写真の追加・削除を実装"
```
